# Leave unset for local dev and the Docker Compose stack (same-origin via Vite/nginx proxy).
# Set to the backend's full URL when the frontend is deployed on a different origin than the
# backend (e.g. two separate Render services) - no trailing slash.
# VITE_API_BASE_URL=https://evergreen-bank-api.onrender.com
